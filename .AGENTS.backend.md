# AGENTS.md / CLAUDE.md — ideophone-arena-api

## What this is

Spring Boot backend for **Ideophone Arena**, a gamified 2AFC experiment from an MA thesis on Japanese ideophones and
script iconicity (Dingemanse; McLean et al. 2020). Players hear two real contrastive ideophones and pick which one
matches a given English translation, under one of three script-presentation conditions. Graded course project +
portfolio piece. Correctness of the experimental manipulation outranks features.

**Supersedes** the pre-June-2026 AGENTS.md and its deadline-era scope freezes. The June 5 deadline has passed; this is
now a summer-paced quality project. `docs/backend-contract.md`
remains the API authority; `docs/phase-2-api-plan.md` is the Phase-2 model (GameMode / PresentationMode /
RoundTemplate / StimulusAsset / RatingAttempt — do not start unprompted).

## Required reading before code changes

`docs/project_guidelines.md` (teacher-facing architecture authority),
`docs/backend-contract.md`, `docs/backend-grading-checklist.md`, `docs/demo-runbook.md`. Append to
`docs/progress-log.md` after each session (existing format: Session goal / Changed / Proof / Result / Commit / Blocker /
Next single task).

## Hard architecture rules (graded)

- Controllers: thin, `ResponseEntity`, DTOs only, `@Valid` on request bodies, no logic, never touch repositories.
- Services: business logic + `@Transactional`; never return entities; no HTTP knowledge.
- All entity↔DTO mapping lives in mappers — never inline in services or as static factories on DTOs.
- Repositories: derived queries or JPQL with bound params only.
- Custom exceptions + `GlobalExceptionHandler`; translate `DataIntegrityViolationException` to 409; never leak stack
  traces.
- Bean Validation on request DTOs. All request endpoints covered (S3, 2026-06-10): `@Valid` on auth/session/answer
  controllers; `responseTimeMs` is `@NotNull @Min(0) @Max(600000)`; redundant null checks removed from `GameService`.
- Constructor injection; no Lombok; no new frameworks or dependencies without explicit approval; ASCII-only comments;
  patch the smallest coherent area.

## Experiment invariants — DO NOT MODIFY without explicit user approval

1. Conditions: `CONDITION_1_SOKUON` (audio only), `CONDITION_2_SOKUON` (congruent script),
   `CONDITION_3_SOKUON` (incongruent script). `difficultyLevel` locked to 1. `TEXT_ONLY` is internal/legacy — never
   externally selectable.
2. **Script display is data, not code.** `ideophones.display_form` is the single source of truth for what kana the
   player sees. Never derive display script at runtime.
3. Stimulus filename prefixes are ground truth, e.g. `a3kh-syakisyaki`:
   pos1 = modality (a/v/i; p = practice), pos2 = pairing index 0–9, pos3 = canonical script (h/k), pos4 = displayed
   script (h/k; u/d = audio-only triangle). pos3==pos4 → congruent; pos3≠pos4 → incongruent. Verification must
   cross-check
   `display_form` against pos4 for every seeded row. Per-word audio files use a 3-character prefix
   (`<p1><p2><p3>-<romaji>.m4a`) — pos4 was dropped because display script now lives in `display_form`, not in
   filenames.
4. Rounds pair contrastive ideophones within the same semantic modality; pairings are canonical per the thesis — do not
   reshuffle.
5. Audio is identical across conditions for a given word; the script manipulation must never alter the audio channel.

## Environment & stack

- Spring Boot 4.0.6, Java 21, Maven wrapper. MySQL localhost:3306, server port **8081**.
- Dev in WSL/Arch. Browsers live under `/mnt/c/` when proofs need one. No Windows-pathed config.
- Schema owned by `src/main/resources/db/init/ideophone_arena.sql`.
  `spring.jpa.hibernate.ddl-auto=validate` in ALL profiles — never `update`/`create`.
- Profiles: base + `local` (gitignored; `application-local_example.properties` is the template).
  `spring.jpa.open-in-view=false` stays false.
- `app.stimuli.locations` may point into the frontend repo (`dist/stimuli`) — intentional cross-repo coupling; frontend
  media resolves through symlinks (`stimuli/<file> -> stimuli/final-stimuli/final-sokuon/<file>`). Don't "fix" without
  asking.
- The legacy Spring-served mini-frontend at `:8081/` was REMOVED (2026-06-10) — the Vite app is the only frontend;
  backend serves API + `/stimuli/**` only. Do not reintroduce it.
- Stale tripwires — never reintroduce: port `8080`, `/api/rounds/next`, `/api/rounds/{id}/answer`.

## Security

- JWT is **deliberately hand-rolled** in `JwtService` (HMAC-SHA256, constant-time compare). Keep it; cover with unit
  tests. No jjwt/nimbus without approval.
- `app.jwt.secret` must come from properties with **no code default** — fail fast if absent.
- BCrypt for passwords; never store or return raw passwords or hashes.
- Roles `ROLE_USER`/`ROLE_ADMIN`; admin endpoints use `hasRole`. CSRF disabled only because stateless JWT.
- CORS explicit in `SecurityConfig` for Vite origins (5173/5174).

## Verification & completion rule

`./mvnw test` must pass before any task is done. A change is complete only with:
what changed / why / files touched / **a proof command and result, or the exact blocker** / next single task. Acceptable
proof: passing tests, successful startup, working curl flow (see `docs/demo-runbook.md`), updated checklist item with
evidence, or a written blocker with exact error output. Never claim done because code looks plausible. When endpoints,
DTOs, ports, auth, CORS, or completion behavior change, update
`docs/backend-contract.md`, `docs/backend-grading-checklist.md`, `docs/demo-runbook.md`.

## Current punch list (June 2026)

- [x] S1 (done 2026-06-10): `display_form`/`canonical_form` columns + seed regeneration (gloss typo fixed, ids
      47/107/167); mapper ships `displayForm`/`canonicalForm`; stimulus assets switched to per-word audio
      (`audio/<p1><p2><p3>-<romaji>.m4a`, ffmpeg stream-copy from u/d mp4s); per-condition mp4 dependency removed from the
      seed; `IdeophoneSeedIntegrityTests` guards the invariants; contract docs updated.
- [x] S1 riders (done 2026-06-10): `ddl-auto=validate` verified in local profile (and the example template now ships
      `validate`); JWT secret fail-fast (no code default, blank guard, `JwtServiceTests`).
- [x] S3 (done 2026-06-10): Bean Validation completed (`responseTimeMs` required, 0-600000); mapping consolidated into
      `GameMapper` (DTO factory removed); duplicate-answer race returns 409 (`saveAndFlush` +
      `DataIntegrityViolationException` translation); score counts session-scoped; completion moved off the GET into
      `submitAnswer` (GET is read-only, same DTO); legacy mini-frontend removed (files + permitAll entries);
      runbook/contract/checklist updated.
- [x] S4 (done 2026-06-11, except practice rounds): `GET /api/admin/stats` behind `hasRole("ADMIN")` (totals /
      per-condition / per-modality aggregates; seed generator emits dev admin `arena_admin`, password in runbook);
      leaderboard paginated (`entries` wrapper + `page`/`size`/`totalElements`/`totalPages`, size capped at 50, username
      tiebreak — BREAKING for the Vite frontend until it reads `.entries`); springdoc 3.0.3 (`/swagger-ui`, `/v3/api-docs`
      public for the demo); SecurityConfig permits the ERROR dispatch so `sendError` 403s are not overwritten to 401 on
      real Tomcat; `AdminStatsHttpTests` + `LeaderboardPaginationHttpTests` (suite: 47).
- [x] Session A (done 2026-06-11): practice rounds (p0-p3 seeded, 204 ideophones / 102 rounds, trial ids unchanged;
      `arena_rounds.is_practice`, `game_sessions.include_practice`/`practice_answered`; optional `includePractice` on
      session start serves 2 practice rounds first with `practice: true`; practice answers give feedback but are never
      persisted and cannot affect score/completion/leaderboard; p-prefix audio extracted + in frontend dist); leaderboard
      reworked to best completed-session metric (`bestSessionCorrect`/`bestSessionAnswered`/`bestSessionAccuracy`,
      completed sessions only — BREAKING for the Vite frontend, needs a rider; pagination wrapper unchanged);
      `scripts/cleanup-test-accounts.sql` idempotently removes `browser_loop_%` accounts; `PracticeRoundHttpTests` +
      extended leaderboard/seed/service tests (suite: 59).
- [x] Rating Lab slice (done 2026-06-20): standalone `ratings` table (`UNIQUE(user_id, ideophone_id)`, nullable
      `session_id`) via `scripts/generate_seed_sql.py` (`--check` clean); `Rating` entity / `RatingRepository` /
      `RatingRequest`+`RatingResponse` DTOs / `RatingMapper` / `RatingService` / `RatingController`. `POST /api/ratings`
      (authenticated, `@Valid`, 1-7 rating, `saveAndFlush`+`DataIntegrityViolationException` -> 409 duplicate, 404
      unknown ideophone, 403/404 unowned/unknown `sessionUuid`) returns 201 with the rating DTO; `GET
      /api/game/me/ratings` returns the caller's own ratings. Standalone only — NOT the Phase-2 model; divergence stat
      not computed. `RatingHttpTests` (suite: 72). Contract/runbook/checklist updated.
- [x] Session B (done 2026-06-12): deterministic per-session shuffle — `game_sessions.shuffle_seed` (SecureRandom,
      never exposed) derives round order, target identity (extension beyond the thesis's parity-fixed targets), target
      side, and a reserved meaning-order draw via `RoundShuffler` (spec is a compatibility contract in
      `docs/backend-contract.md`; pair second = higher ideophone id; practice stream = seed+1). Correctness judged
      against the derived target; `player_answers.target_ideophone_id` stores it (per-target analytics);
      `arena_rounds.correct_ideophone_id`/`prompt` are thesis documentation only, unread in serving. DTO shapes
      unchanged, zero frontend changes; restart continuity proven live. `RoundShufflerTests` +
      `ShuffledSessionHttpTests` (suite: 69).
- [x] NIL-86 (done 2026-07-08): stimulus-expansion pipeline. `generate_seed_sql.py` ingests 21 signed-off expansion
      pairs (workbook `docs/research/data/stimulus-expansion-*.csv`, minus H2/H3 which reuse VISUAL thesis words on the
      Haptic floor — deferred to NIL-57) into `words`/`presentations`/`pairings` but **no trials**, so they are dark
      inventory (ADR-3: a pairing without a trial serves in zero live pools). Adds 34 words (68->102), 102 presentations
      (204->306), 21 pairings (34->55); trials stay 34. Fills the A6 columns: `signoff_ref` = `stimulus-expansion-signoff.xlsx#<pair>`,
      `approved_at` = 2026-07-02, and `foil_distance` computed via the SPEC-free-form-entry §5 feature metric (Decimal,
      deterministic; validation-only covariate, never a gameplay input). `HAPTIC` added to the `Modality` enum for the
      4 clean Haptic pairs. New generator assertions: pair-provenance homogeneity (all jpn audio is `ja-JP-Wavenet-B` TTS)
      beside the invariant-4 same-modality check. Emits `scripts/generate_tts_audio.py` + `scripts/tts-manifest.json`
      (Nils runs the GCP batch; this session does not call GCP) and `docs/research/phonology-golden.json` +
      `scripts/generate_phonology_golden.py` (ADR-8.2 Python side; Java parity owed to NIL-62). `--check` byte-stable
      (thesis rows untouched); darkness proven live (30 rounds / 60 ratable / no HAPTIC bucket); `IdeophoneSeedIntegrityTests`
      extended incl. `expansionPairingsAreDarkWithNoTrials` (suite: 98).
- [x] NIL-60 (done 2026-07-08): A/V/I expansion go-live. Audited the 34 TTS clips (ja-JP-Wavenet-B) — all exist,
      named per invariant 2, ffprobe aac/mono/24 kHz/nonzero. `generate_seed_sql.py` flips the trial-emission filter so
      the 17 A/V/I expansion pairs emit trials (`source != EXPANSION or modality != HAPTIC`); the 4 HAPTIC pairs stay
      dark (no served mode until the Touch floor, NIL-41/42). Expansion trials carry `correct_word_id = NULL` (no thesis
      target; served target is shuffle-derived, unread for CHOOSING). Scored rounds/session 30->47, trials 34->51,
      ratable pool 60->86; zero DTO/endpoint shape changes. `--check` byte-stable (only 17 trial rows + a comment).
      `IdeophoneSeedIntegrityTests` reworked (`hapticExpansionStaysDarkWhileAviExpansionServes`,
      `trialsAreFortySevenScoredPlusFourPractice`); GameLoop/Practice/RatableWords HTTP tests de-hardcoded off 30.
      Live: 47-round session completed, expansion word `urouro` served with audio 200 audio/mp4, ratable=86,
      leaderboard thesis_p-excluded (suite: 98). The 34 clips copied into web `dist/stimuli/audio` (interim media step).
- [x] NIL-88 (done 2026-07-09): api patch pass, three behaviour-internal fixes, zero DTO/endpoint shape change.
      **A10 dev-profile exemption**: `ReservedUsernamePrefixValidator` takes a constructor `@Value` flag and skips the
      rejection when true. The single source is the new tracked `application-automation.properties`, loaded only under
      the `automation` profile (`./mvnw spring-boot:run -Dspring-boot.run.profiles=local,automation`). A10 stays
      pre-deploy-hard: compose sets no `SPRING_PROFILES_ACTIVE`, so the container stays on `local` and never reads the
      file; `./mvnw test` runs under `local` alone, which is what lets `RegistrationHttpTests` keep proving the guarded
      default. (As shipped the skip was wholesale; **superseded by NIL-90 below** — it is now `browser_loop_`-only and
      the property is `app.automation.allow-browser-loop-registration`.)
      `GET /api/game/me/ratings` gains the `...RatedAtDescIdDesc` tie-break (mirrors productions; proven red on the
      old query with a fabricated same-second tie). `AuthService` email fold takes `Locale.ROOT` — the last bare
      `toLowerCase()` in `src/main/java`. Suite 155 -> 157. **NIL-42's sanctioned browser loop is green** at 1280 and
      375px (49 answered = 47 scored + 2 practice, 0 console errors). Three stale harness assertions fixed in web
      `verify-browser-loop.mjs` (all stranded past the A10 wall, so never executed since NIL-41/NIL-60): the
      `difficultyLevel:1` check (A3 removed the field), the 40-round cap, and the `Round 1 / 30` total.
      Found here, fixed in NIL-89 below: `App.tsx DEMO_TOTAL_ROUNDS = 30` drove the CHOOSING progress display.
- [x] NIL-89 (done 2026-07-09): `GameSessionResponse` gains **`totalRounds`** — the scored rounds a session will
      serve, taken off the `RoundSource` seam so it is mode-aware (CHOOSING = 47; LADDER = the floor's pair count),
      practice excluded. `GameMapper.toSessionResponse(session, totalRounds)` takes the count from `GameService`
      (mappers never touch repositories). Additive; nothing removed. Web `App.tsx` drops the hardcoded
      `DEMO_TOTAL_ROUNDS = 30` and passes `session.totalRounds`; `GameSessionResponse.totalRounds` is required in
      `src/api/types.ts`. **The bug it fixes:** because `TrialPlayer` clamps with `Math.min(totalRounds, ...)`, a
      too-small total froze the counter at "Round 30 / 30" and pegged the bar at 100% (`aria-valuemax=30`) for the
      last 17 of 47 scored rounds. Guarded three ways: backend asserts `totalRounds` equals
      `findScoredChoosingTrials().size()` and, on the ladder, equals the rounds actually served; a `TrialPlayer`
      vitest counts past 30; and `verify-browser-loop.mjs` now asserts the *displayed denominator equals the scored
      rounds served* — a relationship, not a literal, so it cannot rot the way the old `Round 1 / 30` check did.
- [x] NIL-90 / NIL-91 / NIL-92 (done 2026-07-09): the three follow-ups NIL-88/89 filed.
      **NIL-90** — the A10 automation exemption is narrowed to `browser_loop_` only; `thesis_p*` is rejected under every
      profile (it is the one prefix read back as data, by `/api/research/thesis/divergence`). Property renamed to
      `app.automation.allow-browser-loop-registration` so the name matches the behaviour. Prod is unreachable **four**
      ways now: `@Value` default false · compose sets no `SPRING_PROFILES_ACTIVE` · `.dockerignore` keeps
      `application-automation.properties` out of the image (so the key has no source there even under the profile) ·
      thesis_p is never exempt. Proven red: the old wholesale skip returned 201 for `thesis_p99`.
      **NIL-91** — new `LadderTrials` component owns the single predicate "which trials does this floor serve";
      `validateLadderStart` and `LadderRoundSource` both ask it, so a floor can no longer validate and then serve zero
      rounds. `GameService` drops `LadderFloors`. This also exposed an **inert Mockito stub** (a fixture trial under
      pairCode `"code"` that `LadderRoundSource` silently discarded). Suite 158 -> 163.
      **NIL-92** — `verify-browser-loop.mjs` collects verdict assertions (`check()`, 33 of them) instead of throwing at
      the first, and wraps the independent proof stages (`runStage`) so a broken ladder walk cannot hide console
      errors. Navigation/timeout/prerequisite failures still throw — nothing after them is meaningful. A stage that
      drives the UI marks `domStateTrusted = false` on failure, and the live-DOM checks (muted media, stale controls,
      overflow) are then skipped rather than measured against an unknown view. Proven: two injected failures were both
      reported in one run, which still completed all 49 rounds and emitted its summary.
